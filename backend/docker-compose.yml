services:
  redis:
    image: redis:alpine
    container_name: portofolio_redis
    ports:
      - '6379:6379'
    volumes:
      - portofolio_redis_data:/data
    restart: unless-stopped
volumes:
  portofolio_redis_data:
